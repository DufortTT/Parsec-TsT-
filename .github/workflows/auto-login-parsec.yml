name: Connect GitHub Runner to Parsec

on: 
  workflow_dispatch:
    inputs:
      email:
        description: 'E-mail do Parsec'
        required: true
      password:
        description: 'Senha do Parsec'
        required: true

jobs:
  connect-parsec:
    runs-on: windows-latest  # Escolha Windows, pois o Parsec depende de GUI

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Install Python Dependencies
      run: |
        pip install -r requirements.txt

    - name: Install Parsec
      run: |
        choco install parsec -y --ignore-checksums

    - name: Launch Parsec and Login
      run: |
        python scripts/parsec_automation.py --email "${{ github.event.inputs.email }}" --password "${{ github.event.inputs.password }}"

    - name: Keep Runner Active
      run: |
        timeout /t 3600
        
